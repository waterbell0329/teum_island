# 틈 아일랜드 (Teum Island) - 프로젝트 요약

> 원티드 AI Championship 2026 출품작 | 제출마감: 9월 20일(일)
> 팀: 임수종(개발) / 김채영(리서치·문서·아트)
> 이 문서는 Claude Code가 개발을 이어받을 때 컨텍스트로 쓰는 요약본입니다.

---

## 1. 한 줄 소개

**앱 이름: 틈 아일랜드 — 부제: "하루의 작은 쉼표"**

사람에게 받은 스트레스를 풀고 싶은데 사람에게 털어놓기 힘든 2-30대 사회초년생을 위한 힐링 게임 앱.
유저가 하루 이야기를 "먹이면" 요정 **얼룩이**가 그 말을 순화(정화)해서 편지로 돌려주고,
동시에 펫(캐릭터)에게 먹이로 줘서 펫이 먹고 성장(레벨업)한다.

**슬로건**: "거친 말은 걸러지고, 마음만 남아요"
**핵심 차별점**: 단순 위로가 아니라 "정화 + 강점 발견"을 AI가 수행 → 캐릭터 성장으로 서사적 누적

---

## 2. 캐릭터

| 이름 | 역할 |
|---|---|
| (펫, 기본이름 미정 - 로그인 시 유저가 변경 가능) | 정화된 말/먹이를 먹고 자라는 순수한 동물+식물 결합형 캐릭터, 인간나이 7~8세 지능 |
| **얼룩이** (요정) | 유저 말을 듣고 정화→편지로 변환, 강점 발견 역할. 캐릭터 오른쪽 위에 미니 아이콘으로 항상 떠있음 |

- 펫 성격: 순수하고 단순하고 귀여움, 좋은 말만 먹고 자람
- 요정 말투: 다정하고 발랄하지만 존중하는 어조 (성인 대상이라 유치하지 않게)
- **캐릭터는 진화(외형 3단계)가 아니라 "옷 갈아입기"로 레벨업 표현** — 몸통/눈/입/팔/다리는 고정 1세트, 레벨에 따라 옷만 교체 (옷 디자인은 스트레치, MVP는 기본 몸만)
- 캐릭터 색상 베리에이션: 스타팅 시 3가지 색상 중 택1 (형태는 동일, 색만 다름 — 아트 부담 최소화하며 "선택하는 느낌" 제공)

---

## 3. 전체 화면 흐름

```
앱 실행 → 스플래시(로고) → 구글 로그인 → 로딩
→ [최초 유저] 닉네임 정하기 → 펫+요정 등장 → 먹이주기 튜토리얼 → 펫 이름 짓기 → 홈
→ [기존 유저] 바로 홈
```

## 4. 홈 화면 레이아웃

| 위치 | 요소 |
|---|---|
| 좌상단 | 설정 아이콘 + 유저 이름/정보 |
| 중앙 상단 | 캐릭터(펫) 이름 |
| 중앙 | 캐릭터, 배경은 섬(블러 처리) |
| 캐릭터 오른쪽 위 | 요정(얼룩이) 미니 아이콘 — 누르면 편지 보관함 |
| 캐릭터 위 겹침 | 퀘스트 미리보기 (반투명 패널) |
| 캐릭터 바로 밑 | 레벨 숫자 + 하늘색 게이지바 |
| 그 아래 | "오늘 하루는 어땠냐?" 버튼 → 입력 진입 |
| 하단 네비 (3개) | 홈 / 퀘스트 / 옷장(인벤토리) — **먹이함 없음** (먹이는 소모품, 컬렉션 아님) |

**하루 여러 번 기록 가능** (제한 없음)

---

## 5. 인터랙션 흐름 (메인 루프)

1. "오늘 하루는 어땠냐?" 버튼 → 2가지 입력 경로 선택
   - **경로 A**: "편하게 쓰기" (자유텍스트)
   - **경로 B**: "빠르게 골라서 전하기" (상황5 + 감정12 + 강도3 + 선택적 한줄설명)
2. 요정(얼룩이) 등장 (반짝이는 소환 연출) → 듣는 포즈 (이때 AI 파이프라인 호출)
3. 요정이 편지로 변환 → 유저에게 전달 (순화 + 강점발견 포함, 3단구성)
4. 요정이 동시에 먹이로 변환 → 펫에게 전달 (편지=먹이, 같은 내용의 다른 표현)
5. **펫이 손을 뻗어 먹이를 캐치 → 입으로 가져가 와그작 씹음 (부스러기 흩어지며 소멸, 컬렉션 아님)**
6. XP 상승 이펙트 + (조건부) 레벨업 시 캐릭터가 좋아하며 한바퀴 돌거나 요정이 주변 회전
7. 세션 종료 (1턴 = 1사이클, 여러 턴 대화는 스트레치)

**긍정 감정 입력 시**: 위로 대신 절제 없이 격하게 축하 + 함께 기뻐함

---

## 6. 감정/콘텐츠 체계

**감정 12개 (버튼 UI 기준, 최종 확정)**
- 부정(7): 분노, 억울함, 무기력, 막막함, 서운함, 불안함, 지침(번아웃)
- 긍정(5): 뿌듯함, 설렘, 안심, 감사함, 홀가분함

**상황 카테고리 (5개)**: 직장/알바, 인간관계, 학업/진로, 미래불안, 기타

**강도 (3단계)**: 약간 힘듦(좋음) / 많이 힘듦(좋음) / 정말 힘듦(좋음)

**퀘스트**: 전날 AI가 감정 데이터 기반으로 3~4개 추천하는 기획 원안은 보류 (미구현). 대신 **기록할 때마다 감정에 맞춰 규칙기반으로 즉석 추천하는 가벼운 루틴 퀘스트**로 방향을 잡음(AI 호출 없이 카테고리별 퀘스트풀에서 랜덤 2~3개, 완료 저장은 안 함) — 2026-09-07, `quest_service.py`/`quest_bank.py`로 구현 완료. 완료 보상은 여전히 캐릭터 성장(XP)

---

## 7. AI 파이프라인 (4단계)

| 단계 | 역할 | 방식 |
|---|---|---|
| 1. 감정 분류 | 자유텍스트 입력 → 감정 유형 분류 (구조화입력은 유저가 이미 선택했으니 스킵) | **직접 학습시킨 모델** (LoRA 또는 자체 분류기, 아래 "AI 모델 현황" 참고) |
| 2. 구조화된 분석 (신설) | 상황요약/핵심감정/숨은강점후보/맥락키워드를 JSON으로 추출. 강점은 후보군(인내심/책임감/유연성/경계설정/노력/회복탄력성/배려심) 중에서만 선택 | LLM API |
| 3. 공감 응답 생성 | 2단계 분석 + 개인화 프로필 참고해서 편지 작성. 3단구성 강제, 금지표현 필터 | LLM API |
| 4. 프로필 요약/XP 산정 | 누적 기록 요약 → 개인화 프로필 업데이트 + XP 계산 | LLM API + 로직 |

**LLM: Google Gemini** (Anthropic API는 크레딧 결제 이슈로 전환함). `gemini-2.0-flash`가 서비스 종료(404)되어 `gemini-flash-latest` 별칭으로 교체 완료, SDK도 `google.generativeai`(지원종료) → `google-genai`로 전면 교체 완료 (2026-09-07).
**⚠️ 무료 티어 쿼터 이슈**: 짧은 시간당 요청 수 제한이 매우 낮아(`gemini-3.8-flash` 기준 20회) 개발 중에도 금방 429(RESOURCE_EXHAUSTED)에 걸림 — 데모 당일 리스크이므로 **결제(유료 티어) 전환 여부를 팀에서 결정 필요**. 503(일시 과부하)에 대해서는 재시도 로직 추가함.

**금지표현**: "화이팅", "다 잘될거야", "누구나 그래", "긍정적으로 생각해" 등 상투적 위로 금지 → 구체적 상황 언급으로 대체

**강도별 톤**: 약간=가볍고 발랄 / 많이=따뜻하고 차분 / 정말=과도한 명랑함 자제, 진지한 공감 우선

---

## 8. 감정 분류기 (직접 학습 모델) 현황

- **베이스라인 v1**: 5개 상위그룹(분노억울/무기력지침/불안막막/서운/긍정)으로 임베딩(ko-sroberta-multitask)+로지스틱회귀, 정확도 100%였으나 테스트셋이 너무 작아 신뢰 어려움
- **LoRA v1 (Qwen2.5-1.5B-Instruct)**: 12개 세부감정, 데이터 1,200개(템플릿 조합), 홀드아웃 테스트 정확도 **53%**
  - 문제: "지침"이 다른 부정감정을 과하게 흡수, 긍정감정 3개(뿌듯함/설렘/홀가분함) 상호 혼동, 목록에 없는 단어("초조") 생성
- **LoRA v2 (진행 중/완료 확인 필요)**: 데이터를 2,400개(클래스당 200개)로 확장, 문장 구조 다양화(헷갈린 쌍 타겟팅), 프롬프트에 "목록에 없는 단어 금지" 규칙 추가, 추론 시 라벨 포함 매칭 안전장치 추가
  - **다음 할 일**: v2 학습 완료 확인 → 구글드라이브 저장 확인 (`emotion_lora_adapter_v2`) → `holdout_test_set.csv`로 재검증 → 정확도 기준(70%↑ 확정 / 40~70% 추가보강 / 40%↓ 베이스라인 회귀)으로 판단

**⚠️ Colab 세션 주의사항**: 세션 재시작 시 파일/변수 다 날아갈 수 있음 (오래 방치하면 완전히 새 런타임 배정됨). 학습 끝나면 **즉시** 구글드라이브에 저장할 것.

---

## 9. XP/레벨 시스템

**XP 지급**: 기록 1회 기본 10 / 강도"많이" 15 / 강도"정말" 20 (긍정감정도 동일 기준)

**레벨 임계값**: `다음레벨 필요XP = 이전레벨 필요XP × 1.15` (반올림), 무한레벨, 시작값 100XP
- 0단계: 튜토리얼 중 (XP 시스템 미적용)
- 1레벨부터 정상 작동

**레벨업 연출**: 펫이 좋아하며 한바퀴 돌거나 요정이 근처에서 회전. 벌칙 없음(Finch 철학) — 안 써도 XP 안 깎이고 캐�릭터가 화내지 않음

**⚠️ 데모 이슈**: 실제 곡선대로면 상위 레벨 도달까지 매우 오래 걸림 → 발표 데모용 XP 배율 증폭 모드 또는 시드 데이터 테스트 계정 준비 필요 (아직 미구현)

---

## 10. 안전장치

**위기신호 감지**: 자해·자살 관련 키워드 감지 시 일반 파이프라인 스킵하고 안전 문구로 강제 전환
- 안내: "혼자 감당하기 힘든 이야기같아. 지금 바로 도움받을 수 있는 곳이 있어." + 24시간 자살예방상담전화 **109** (2024년 통합) / 정신건강상담전화 **1577-0199**

**"상담 대체 아님" 고지**: 온보딩 마지막에 캐릭터 톤으로 자연스럽게 + 설정 메뉴 상시 노출

---

## 11. 기술 스택 & 현재 진행 상태

| 영역 | 스택 | 상태 |
|---|---|---|
| 프론트엔드 | Next.js (React) + Tailwind + Framer Motion | 프로젝트 생성 완료 (`npx create-next-app`), 화면 코딩 전 (아트 대기 중) |
| 백엔드 | Python FastAPI | Supabase 연동 완료 + 실제 DB로 온보딩/기록/위기감지/XP 전 구간 검증 완료 (2026-09-07) |
| DB | Supabase (Postgres + pgvector) | 스키마 적용 완료, 실제 데이터 흐름 테스트 완료. `food_collection`은 코드에서 미사용 처리, 테이블 자체 삭제는 미해결 참고 |
| LLM | Google Gemini (`gemini-flash-latest`, `google-genai` SDK) | 연동 완료 (analysis_service.py, letter_service.py, profile_service.py), 무료 쿼터 이슈 있음 |
| 감정분류기 | 자체 학습 (LoRA on Qwen2.5-1.5B) | v2 학습/검증 진행 중. 코드(classifier_service.py)는 지연 임포트로 개선했으나 모델 파일 자체는 아직 없음 |
| 개발방식 | Claude Code 활용, 핵심로직은 직접 이해 후 진행 | - |

**⚠️ 개발환경 주의**: 프로젝트가 OneDrive 동기화 폴더 안에 있어서 `venv`처럼 파일 수가 많은 디렉토리는 디스크 I/O가 느려질 수 있음. 무거운 패키지(torch 등)를 모듈 최상단에서 import하면 서버 기동마다 수십 초씩 걸리니, 꼭 필요한 시점에 지연 임포트(lazy import) 하는 걸 기본으로 할 것.

**백엔드 폴더 구조**:
```
backend/
  app/
    main.py
    core/
      config.py            # .env 값 로드 (SUPABASE_URL, GOOGLE_API_KEY 등)
      supabase_client.py   # Supabase 클라이언트 싱글톤
    schemas/
      emotion.py           # user_id, is_crisis 포함된 요청/응답 모델
    services/
      classifier_service.py   # 1단계 (지연 임포트로 개선, 모델 파일은 아직 없음)
      analysis_service.py     # 2단계 (Gemini) — 자유텍스트 강도 추론도 여기서 같이 함
      letter_service.py       # 3단계 (Gemini)
      xp_service.py           # 4단계 XP/레벨 계산
      profile_service.py      # 4단계: 감정 프로필 요약 배치 (5번째 기록마다 갱신)
      quest_service.py        # 규칙기반 루틴 퀘스트 선정 (AI 호출 없음)
      safety_service.py       # 위기신호 감지
      db_service.py           # Supabase CRUD 함수 모음
    data/
      quest_bank.py            # 카테고리별 루틴 퀘스트 문구 (감정->카테고리 매핑 포함)
    routers/
      emotion_logs.py      # POST /emotion-logs (4단계 파이프라인 + 루틴 퀘스트 오케스트레이션)
      users.py              # 유저 조회/온보딩완료
      quests.py             # 보류된 옛 퀘스트(quests/user_quests 테이블) 조회 스텁 -- quest_service로 대체됨
```

**DB 스키마 (Supabase, 적용됨)**: users, emotion_logs, emotion_profiles(pgvector 포함), crisis_flags, quests, user_quests, food_collection
- `users.id`는 `auth.users.id`를 참조하는 FK — 구글 로그인(Supabase Auth)으로 만들어진 유저가 아니면 `users` 행을 만들 수 없음 (테스트 시 유의)
- `food_collection`은 코드에서 사용하지 않도록 처리 완료(먹이 소모품화 결정 반영). 테이블 자체 DROP은 아래 "미해결" 참고

---

## 12. 아트 에셋 요청 목록 (김채영 담당, 최신 기준)

**화풍/컬러**: 아직 미확정 (플랫벡터 추천했으나 최종 결정 필요). 컬러는 초록/베이지/갈색/흰색/노란색 기준

### 캐릭터
- 펫 몸통(기본형, 옷 없음) 1장, 눈 1~2장, 입(닫힘/벌림) 2장, 팔(내림/뻗음) 2장, 다리 1~2장
- 색상 베리에이션 3종 (같은 형태, 색만 다르게)
- 요정(얼룩이) 전신 1장 + 미니 아이콘 버전 1장

### 배경
- 섬 배경 원본 1장 (블러는 코드 처리)
- 하늘/구름 레이어 2장 (온보딩 확대연출용)

### 아이콘
- 하단네비 2개(홈/퀘스트) — 옷장 아이콘 포함하면 3개
- 먹이 아이콘 12개 (감정별, 소모품이라 컬렉션북은 없지만 아이콘 자체는 필요)
- 편지 봉투 아이콘 1개
- 설정 아이콘 1개

### 로그인/온보딩
- 서비스 로고("틈 아일랜드")
- 스플래시 화면

### 스트레치 (나중에)
- 옷/의상 세트 (레벨업 시 갈아입을 옷, 몇 벌인지 미정)
- 옷장 인벤토리 화면 UI
- 펫 표정 다양화

---

## 13. 애니메이션 참고 (프로토타입 완성, HTML 데모로 확인함)

**핵심 원칙**: 스쿼시앤스트레치 + 레이어 분리(몸통/눈/입/팔 별도) + 바운스 이징 곡선 → 그림 실력보다 타이밍이 8할

**펫 먹이 캐치 시퀀스**:
1. 대기 → 2. 예비동작(웅크림) → 3. 팔 뻗어서 캐치 → 4. 손을 입으로 이동 → 5. 와그작 씹기(입 2연타 모션 + 부스러기 흩어지며 소멸) → 6. 셀레브레이션(양팔+다리 흔들며 반짝이 이펙트)

Framer Motion으로 이식 시 keyframe 구조 그대로 재사용 가능 (offset 기반 다단계 애니메이션)

---

## 14. 아직 미해결 / 확인 필요한 것

지난 세션(2026-09-07)에서 개발 관련 항목 3개를 구현·검증 완료함. 실제로 해결된 것과, 여전히 남은 것을 구분해서 정리.

### ✅ 이번에 해결됨 (2026-09-07)

- [x] 자유텍스트 입력 시 감정 강도 추론 로직 → 2단계 구조화 분석(Gemini) 호출에 `intensity` 필드를 함께 받도록 확장. 추가 API 호출 없이 구현 (`analysis_service.py`)
- [x] 감정 프로필 요약 배치 로직 → 5번째 기록마다 한 번씩 최근 기록을 요약해 `emotion_profiles`에 갱신 (`profile_service.py`, `UPDATE_EVERY_N_LOGS = 5`)
- [x] `food_collection` 테이블 관련 코드 정리 → 백엔드 코드에서 완전히 미사용 처리 (실제 DB 테이블 DROP은 아래 항목 참고)

### 🔧 개발 항목 (Claude Code가 이어서 진행 가능)

- [ ] **`food_collection` 테이블 실제 삭제** — 데이터 0건 확인함, 삭제하기로 결정됨. Supabase는 REST API(서비스 키)로 DDL 실행이 안 되기 때문에 Supabase 대시보드 → SQL Editor에서 아래 한 줄 직접 실행 필요:
  ```sql
  DROP TABLE IF EXISTS public.food_collection;
  ```
- [ ] **Gemini 무료 쿼터 이슈** — 개발 중에도 429(RESOURCE_EXHAUSTED)에 자주 걸림 (짧은 시간당 20회 제한). 결제(유료 티어) 전환 여부 결정 필요 — 데모 당일 실패 리스크 있음
- [ ] 데모용 XP 배율 증폭 모드 / 시드 데이터 계정 — 아직 미구현
- [ ] 감정 분류기(LoRA v2) 학습/검증 상태 확인 — 8번 항목 참고, 모델 파일 자체가 아직 없어서 자유텍스트 경로가 실제로는 동작 안 함

### 📋 팀/기획 결정 필요 (개발로 해결 불가 — 임수종/김채영이 정할 것)

- [ ] 펫 기본 이름 확정
- [ ] 회의시간 (22시 vs 10시, 노션에 둘 다 남아있음)
- [ ] 대회 제출 형식 확인 (문서/영상/링크)
- [ ] 팀장 정보 (상금 수령 관련)
- [ ] 발표 스크립트 5개 항목 (문제/한계/솔루션/AI역할/차별점)
- [ ] 경쟁사 리서치 필터링표 마무리

---

## 15. 대회 정보

- 원티드 AI Championship 2026
- 제출 마감: 9월 20일(일)
- 9월 21일부터 제출작 공개 + 투표 시작
- 참가자격: 원티드 회원, 만 19세 이상, 팀 최대 5명

---

*이 문서는 대화형 기획 세션에서 나온 결정사항을 정리한 것으로, 노션 페이지("AI Championship 2026 - 프로젝트 보드")와 구글독스("AI Championship 2026 - 프로젝트 문서")에도 유사한 내용이 있으나 이 문서가 가장 최신 상태를 반영함(틈 아일랜드, 얼룩이, 옷갈아입기 방식, 먹이 소모품화 등 최근 변경사항 포함).*
